#Использовать asserts
#Использовать tempfiles
#Использовать "utils"
#Использовать "../.."

#Область ОписаниеПеременных

Перем НакопленныеВременныеФайлы; // фиксация накопленных времнных файлов для сброса

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

&Тест
Процедура ТестДолжен_РазобратьТекущуюКонфигурациюНаИсходникиIbcmd() Экспорт

	// Дано
	Исполнитель = Новый Тест_ИсполнительКоманд("decompile");
	КаталогSrc = Исполнитель.ПутьТестовыхДанных("cf");
	Исполнитель.УстановитьКонтекстИБИзФайловКонфигурации(КаталогSrc);
	Исполнитель.ДобавитьПараметр("--out", "src");
	Исполнитель.ДобавитьФлаг("--current");
	Исполнитель.ДобавитьФлаг("--ibcmd");

	// Когда
	Исполнитель.ВыполнитьКоманду();

	// Тогда
	Исполнитель.ОжидаемЧтоВыводСодержит("Используется ibcmd");
	Исполнитель.ОжидаемЧтоВыводСодержит("Разборка конфигурации текущей ИБ на исходники завершена.");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Configuration.xml");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Catalogs/Справочник1.xml");

КонецПроцедуры

&Тест
Процедура ТестДолжен_РазобратьФайлКонфигурацииНаИсходникиIbcmd() Экспорт

	// Дано
	Исполнитель = Новый Тест_ИсполнительКоманд("decompile");
	ФайлCf = Исполнитель.ПутьТестовыхДанных("1cv8.cf");
	Исполнитель.ДобавитьПараметр("--in", ФайлCf);
	Исполнитель.ДобавитьПараметр("--out", "src");
	Исполнитель.ДобавитьФлаг("--ibcmd");

	// Когда
	Исполнитель.ВыполнитьКоманду();

	// Тогда
	Исполнитель.ОжидаемЧтоВыводСодержит("Используется ibcmd");
	Исполнитель.ОжидаемЧтоВыводСодержит("Разборка cf-файла на исходники завершена.");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Configuration.xml");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Catalogs/Справочник1.xml");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОбновитьСуществующуюВыгрузкуТекущейКонфигурацииКонфигуратор() Экспорт

	// Дано
	// Каталог исходников уже не пуст, а ConfigDumpInfo.xml в нем описывает состояние ИБ (issue #717)
	Исполнитель = Новый Тест_ИсполнительКоманд("decompile");
	КаталогИсходников = ОбъединитьПути(Исполнитель.КаталогКоманды(), "src");
	ФС.КопироватьСодержимоеКаталога(Исполнитель.ПутьТестовыхДанных("cf"), КаталогИсходников);
	Исполнитель.УстановитьКонтекстИБИзФайловКонфигурации(КаталогИсходников);
	Исполнитель.ВыгрузитьВФайлСостояниеКонфигурации(КаталогИсходников);

	Исполнитель.ДобавитьПараметр("--out", "src");
	Исполнитель.ДобавитьПараметр("--versions", ОбъединитьПути("src", "ConfigDumpInfo.xml"));
	Исполнитель.ДобавитьФлаг("--current");

	// Когда
	Исполнитель.ВыполнитьКоманду();

	// Тогда
	Исполнитель.ОжидаемЧтоВыводНеСодержит("Используется ibcmd");
	Исполнитель.ОжидаемЧтоВыводСодержит("Обновляем существующую выгрузку");
	Исполнитель.ОжидаемЧтоВыводСодержит("Разборка конфигурации текущей ИБ на исходники завершена.");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Configuration.xml");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Catalogs/Справочник1.xml");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОбновитьСуществующуюВыгрузкуТекущейКонфигурацииБезФайлаВерсийКонфигуратор() Экспорт

	// Дано
	// --versions не указан, но в каталоге выгрузки уже лежит ConfigDumpInfo.xml
	Исполнитель = Новый Тест_ИсполнительКоманд("decompile");
	КаталогИсходников = ОбъединитьПути(Исполнитель.КаталогКоманды(), "src");
	ФС.КопироватьСодержимоеКаталога(Исполнитель.ПутьТестовыхДанных("cf"), КаталогИсходников);
	Исполнитель.УстановитьКонтекстИБИзФайловКонфигурации(КаталогИсходников);
	Исполнитель.ВыгрузитьВФайлСостояниеКонфигурации(КаталогИсходников);

	Исполнитель.ДобавитьПараметр("--out", "src");
	Исполнитель.ДобавитьФлаг("--current");

	// Когда
	Исполнитель.ВыполнитьКоманду();

	// Тогда
	Исполнитель.ОжидаемЧтоВыводСодержит("Обновляем существующую выгрузку");
	Исполнитель.ОжидаемЧтоВыводСодержит("Разборка конфигурации текущей ИБ на исходники завершена.");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Configuration.xml");

КонецПроцедуры

&Тест
Процедура ТестДолжен_ОбновитьСуществующуюВыгрузкуТекущейКонфигурацииIbcmd() Экспорт

	// Дано
	// ibcmd не позволяет сочетать --base и --sync: существующая выгрузка должна обновляться через --sync
	Исполнитель = Новый Тест_ИсполнительКоманд("decompile");
	КаталогИсходников = ОбъединитьПути(Исполнитель.КаталогКоманды(), "src");
	ФС.КопироватьСодержимоеКаталога(Исполнитель.ПутьТестовыхДанных("cf"), КаталогИсходников);
	Исполнитель.УстановитьКонтекстИБИзФайловКонфигурации(КаталогИсходников);
	Исполнитель.ВыгрузитьВФайлСостояниеКонфигурации(КаталогИсходников);

	Исполнитель.ДобавитьПараметр("--out", "src");
	Исполнитель.ДобавитьПараметр("--versions", ОбъединитьПути("src", "ConfigDumpInfo.xml"));
	Исполнитель.ДобавитьФлаг("--current");
	Исполнитель.ДобавитьФлаг("--ibcmd");

	// Когда
	Исполнитель.ВыполнитьКоманду();

	// Тогда
	Исполнитель.ОжидаемЧтоВыводСодержит("Используется ibcmd");
	Исполнитель.ОжидаемЧтоВыводСодержит("Обновляем существующую выгрузку");
	Исполнитель.ОжидаемЧтоВыводСодержит("Разборка конфигурации текущей ИБ на исходники завершена.");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Configuration.xml");
	Исполнитель.ОжидаемЧтоФайлСуществует("src/Catalogs/Справочник1.xml");

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ПередЗапускомТеста() Экспорт
	
	НакопленныеВременныеФайлы = ВременныеФайлы.Файлы();
	
КонецПроцедуры

Процедура ПослеЗапускаТеста() Экспорт
	
	ВременныеФайлы.УдалитьНакопленныеВременныеФайлы(НакопленныеВременныеФайлы);
	
КонецПроцедуры

#КонецОбласти
